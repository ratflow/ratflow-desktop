#------------------------------------------------
#
# floating windows
#
#------------------------------------------------

${{fdir="$HOME/.config/ratflow/profiles/current/config.d/floating.d" && test -d $fdir && cat $fdir/*.float}}

